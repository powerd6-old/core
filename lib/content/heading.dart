import 'package:powerbook/models.dart';
import 'package:powerd6/models.dart';

class BookHeading {
  static final Heading heading = Heading(
      title: "Core Rulebook",
      subtitle:
          Paragraph(["The core rules of the powerd6 role-playing system."]));
}
